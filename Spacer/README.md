# Автоматическая расстановка пробелов в слитно написанных русских текстах с использованием динамического программирования, частотного словаря и морфемной   классификации на основе BERT.
Проект предназначен для восстановления пробелов в текстах, где они были утеряны (например, при OCR, парсинге или опечатках).   
Используется комбинированный подход: сначала применяется сегментация на основе динамического программирования, затем уточнение с помощью морфемной модели на BERT.  
## Быстрый старт
1. Клонируйте репозиторий  
git clone https://github.com/necoarcreator/samplesML/Spacer.git  
cd Spacer  
2. Установите зависимости  
pip install -r requirements.txt  
3. Запуск  
from main import run_pipe  
run_pipe()  
## Структура проекта   
.    
├── dataset/                  # Трейновый датасет    
  ├── dataset.txt    
├── dictionary/               # Данные: обычный и частотный словари    
  ├── ruscorpora_content.csv  # Спасибо Russian National Corpus за частотный словарь русского языка!    
  ├── russian.txt             # Спасибо Danakt Saushkin за словарь русского языка!    
├── main.py                   # Главный скрипт запуска    
├── models.py                 # Модели: GreedySpacer, DPSpacer, WordValidator    
├── ds_loader.py              # Загрузчик датасета    
├── ru_loader.py              # Загрузчик словарей    
├── unittests.py              # Набор тестов для моделей    
├── .gitignore                # Игнорируемые файлы    
└── README.md                 # Этот файл    
## Принцип работы пайплайна  
1. DPSpacer — сегментирует текст с помощью динамического программирования, опираясь на частотный словарь.  
2. WordValidator (на основе BERT) — проверяет, является ли слово «морфологически правильным» (имеет 1 корень, логичную структуру морфем).  
3. GreedySpacer — fallback: жадная сегментация по максимальному совпадению, если BERT и DP не уверены.  
4. Комбинированный пайплайн — сначала сегментируем DP → проверяем BERT → если слово «плохое» — сегментируем заново → проверяем снова → fallback на жадный алгоритм.  
