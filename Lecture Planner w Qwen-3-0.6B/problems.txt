0. Скорость загрузки видео и их вес -> параллельно запускаю 3 баш-скрипта, чуть ограничиваю время между их запросами, чтобы
не словить блокировку за ddos, загружаю с частотой дискретизации не выше необходимой для whisper, ограничиваю снизу и сверху длину
видео, ограничиваю максимальное число скачанных видео.
Возможное улучшение: поиграть с безопасным числом запущенных скриптов

1. Зацикливание генерации ASR -> использую файнтюнинг на русский язык + постобработка регуляркой на повторения слов и пробелов; явно
указываю, что задача транскрибации на русском языке
Возможное улучшение: покрутить параметры, отвечающие за порог обработки виспером


2. Время обработки одной записи ASR -> это в целом bottleneck пайплайна, использую CUDA, пробую использовать fp16 вместо fp32, 
настроил число батчей под моё железо, увеличил окно контекста до 60 с (для коротких записей было бы плохо), не считаю timestamps сказанного.
Возможное улучшение: скачать whisper.cpp, запустить с ним с обёрткой в виде WSL
3. Ошибки ConnectionError и подобное при обращении к OpenAI GPT-120-oss -> делаю обёртку tenacity.retry, try - except, 
экспоненциальную задержку запросов
Возможное улучшение: понять, как ожидать ответ hf.space правильно - он может "уснуть" за то время, что ASR модель обрабатывает следующий батчей

4. Длинный контекст -> порезать лекции на k частей (с перекрытием) и давать план для них отдельно
Возможное улучшение: резать не по длине, а по семантике - найти на hf маленькую модель, которая бы отмечала, где идёт новая тема и резать тема
например, sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2 -> кос_сим между соседними чанками больше порога -> делим

5. CUDA OOM даже с квантизацией и QLoRA -> обучать не на всей лекции целиком, а на кусках. Надеяться, что качество на инференсе не 
пострадает сильно.

